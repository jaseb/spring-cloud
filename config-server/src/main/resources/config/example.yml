---
# Spring Boot defaults to 8080
server:
  port: 8080

example:
  message: Message from local application.yml

---
spring:
  profiles: zone-jp

example:
  message: '{cipher}TODO: INSERT ENCRYPTED MESSAGE HERE'

---
spring:
  profiles: zone-us

example:
  message: Message from local application.yml - zone-us

